package com.example.recipecasket.model

import android.net.Uri
import android.view.View
import android.widget.EditText
import com.example.recipecasket.R
import com.google.firebase.Firebase
import com.google.firebase.database.FirebaseDatabase
import com.google.firebase.storage.storage

class RecipeGUI(private val rootView: View) {

/*    val nameEditText: EditText = rootView.findViewById(R.id.etName)
    val cuisineEditText: EditText = rootView.findViewById(R.id.etCuisine)
    val ingredientsEditText: EditText = rootView.findViewById(R.id.etIngredients)
    val recipeEditText: EditText = rootView.findViewById(R.id.etRecipe)
    val cookingTimeEditText: EditText = rootView.findViewById(R.id.etCookingTime)*/


}